# @Model["Module"].Name - API

@foreach(var command in Model["Commands"])
{
@:##  @command.Name


@if(command.details.description != null) {
    @command.Synopsis
    @:
    @:
}


@if(command.details.description != null) {
    @foreach(var item in OutString(command.syntax).Trim().Split('\n'))
    {
@:    @item
@:
    }
}
else {
    @foreach(var item in OutString(@command.Synopsis).Trim().Split('\n'))
    {
@:    @item
@:
    }
}

@:
@: [Read more](@(command.Name).md)

}
